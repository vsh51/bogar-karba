@{
    var currentYear = DateTime.UtcNow.Year;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Bogar Karba</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <environment include="Development">
        <link rel="stylesheet" href="~/css/variables.css" asp-append-version="true" />
        <link rel="stylesheet" href="~/css/layout.css" asp-append-version="true" />
        <link rel="stylesheet" href="~/css/components.css" asp-append-version="true" />
        <link rel="stylesheet" href="~/css/checklist-show.css" asp-append-version="true" />
        <link rel="stylesheet" href="~/css/checklist-private.css" asp-append-version="true" />
        <link rel="stylesheet" href="~/css/checklist-editor.css" asp-append-version="true" />
        <link rel="stylesheet" href="~/css/checklist-quick-create.css" asp-append-version="true" />
        <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    </environment>
    <environment exclude="Development">
        <link rel="stylesheet" href="~/css/bundle.min.css" asp-append-version="true" />
    </environment>

    <link rel="stylesheet" href="~/Web.styles.css" asp-append-version="true" />
</head>
<body>
    <header>
        <nav class="bk-navbar navbar navbar-expand-lg" aria-label="Primary">
            <div class="bk-container bk-navbar-inner">
                <a class="navbar-brand" asp-controller="Home" asp-action="Index">Bogar Karba</a>

                <button class="navbar-toggler"
                        type="button"
                        data-bs-toggle="collapse"
                        data-bs-target="#bk-primary-nav"
                        aria-controls="bk-primary-nav"
                        aria-expanded="false"
                        aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="navbar-collapse collapse" id="bk-primary-nav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Index">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Privacy">Privacy</a>
                        </li>
                    </ul>

                    <ul class="navbar-nav align-items-lg-center">
                        @if (User.Identity?.IsAuthenticated == true)
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Checklist" asp-action="Create">Create Checklist</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Author" asp-action="Index">My Checklists</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Checklist" asp-action="Shared">Shared with me</a>
                            </li>
                            <li class="nav-item dropdown">
                                <button id="notification-bell"
                                        class="bk-bell"
                                        type="button"
                                        data-bs-toggle="dropdown"
                                        aria-expanded="false"
                                        aria-label="Notifications">
                                    <i class="bi bi-bell" aria-hidden="true"></i>
                                    <span class="visually-hidden">Notifications</span>
                                    <span id="notification-badge"
                                          class="bk-bell-badge d-none"
                                          aria-hidden="true"></span>
                                </button>
                                <ul id="notification-list" class="dropdown-menu dropdown-menu-end bk-bell-list">
                                    <li class="dropdown-item text-muted small">No notifications</li>
                                </ul>
                            </li>
                            <li class="nav-item dropdown">
                                <button class="bk-user-menu"
                                        type="button"
                                        data-bs-toggle="dropdown"
                                        aria-expanded="false">
                                    <i class="bi bi-person-circle" aria-hidden="true"></i>
                                    <span class="bk-user-name" title="@User.Identity.Name">@User.Identity.Name</span>
                                    <i class="bi bi-chevron-down bk-user-menu__chevron" aria-hidden="true"></i>
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end shadow-sm">
                                    <li>
                                        <form asp-controller="Account" asp-action="Logout" method="post" class="m-0">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="dropdown-item">
                                                <i class="bi bi-box-arrow-right me-2"></i> Logout
                                            </button>
                                        </form>
                                    </li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Account" asp-action="Register">Register</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Account" asp-action="Login">Login</a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main role="main">
        <div class="bk-container">
            @if (TempData["SuccessMessage"] is string successMsg)
            {
                <div class="alert alert-success alert-dismissible fade show mt-3" role="alert">
                    @successMsg
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }
            @if (TempData["WarningMessage"] is string warningMsg)
            {
                <div class="alert alert-warning alert-dismissible fade show mt-3" role="alert">
                    @warningMsg
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }
            @if (TempData["ErrorMessage"] is string errorMsg)
            {
                <div class="alert alert-danger alert-dismissible fade show mt-3" role="alert">
                    @errorMsg
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }

            @RenderBody()
        </div>
    </main>

    <div class="modal fade" id="bk-confirm-modal" tabindex="-1" aria-labelledby="bk-confirm-title" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="bk-confirm-title">Confirm</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p class="mb-0" id="bk-confirm-message"></p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal" id="bk-confirm-cancel-btn">Cancel</button>
                    <button type="button" class="btn btn-primary" id="bk-confirm-ok-btn">Confirm</button>
                </div>
            </div>
        </div>
    </div>

    <div class="bk-toast-stack" id="bk-toast-stack" aria-live="polite" aria-atomic="true"></div>

    <footer class="bk-footer">
        <div class="bk-container">
            <p class="bk-footer-text">&copy; @currentYear Bogar Karba — Technical checklist tracker.</p>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

    <environment include="Development">
        <script src="~/js/site.js" asp-append-version="true"></script>
        <script src="~/js/ui.js" asp-append-version="true"></script>
    </environment>
    <environment exclude="Development">
        <script src="~/js/bundle.min.js" asp-append-version="true"></script>
    </environment>

    @if (User.Identity?.IsAuthenticated == true)
    {
        <script src="~/lib/signalr/signalr.min.js"></script>
        <script src="~/js/notifications.js" asp-append-version="true"></script>
    }
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
